\documentclass[10pt]{article}

\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath,amsthm,amssymb}
\usepackage{xcolor}
\usepackage{cancel}
\usepackage{graphicx}
\usepackage{changepage}
\usepackage{circuitikz}
\usepackage{pgfplots}
\usepackage{physics}
\usepackage{hyperref}
\usepackage{siunitx}
\usepackage{fontspec}
\usepackage{relsize}
\usepackage{subfig}
\usepackage{minted}
\usepackage{pdfpages}
\usepackage{todonotes}
\usepackage{multicol, multirow, booktabs}
\usepackage[breakable]{tcolorbox}
\usepackage[inline]{enumitem}

\theoremstyle{definition}
\newtheorem{problem}{Problem}
\newtheorem{soln}{Solution}

\pgfplotsset{compat=newest}
\usetikzlibrary{lindenmayersystems}
\usetikzlibrary{arrows}
\usetikzlibrary{calc}
\usetikzlibrary{positioning, fit}
\usetikzlibrary{3d, perspective}

\definecolor{incolor}{HTML}{303F9F}
\definecolor{outcolor}{HTML}{D84315}
\definecolor{cellborder}{HTML}{CFCFCF}
\definecolor{cellbackground}{HTML}{F7F7F7}
\newcommand{\ui}{\hat{i}}
\newcommand{\uj}{\hat{j}}
\newcommand{\uk}{\hat{k}}
\newcommand{\ux}{\hat{\mathbf{x}}}
\newcommand{\uy}{\hat{\mathbf{y}}}
\newcommand{\uz}{\hat{\mathbf{z}}}
\newcommand{\uv}[1]{\hat{\bm{{#1}}}}
\newcommand{\pr}[1]{{#1^\prime}}
\newcommand{\pd}[2][]{{\frac{\partial^{#1}}{\partial {{#2}^{#1}}}}}
\newcommand{\dif}[2][]{{\frac{d^{#1}}{d{{#2}^{#1}}}}}
\pgfdeclarelayer{background}  
\pgfsetlayers{background,main}
\AtBeginDocument{\RenewCommandCopy\qty\SI}
\newcommand{\justif}[2]{&{#1}&\text{#2}}
\DeclareMathOperator\Arg{Arg}
\DeclareMathOperator{\sgn}{sgn}

\makeatletter
\newcommand{\boxspacing}{\kern\kvtcb@left@rule\kern\kvtcb@boxsep}
\makeatother
\newcommand{\prompt}[4]{
    \ttfamily\llap{{\color{#2}[#3]:\hspace{3pt}#4}}\vspace{-\baselineskip}
}

\newcommand{\thevenin}[2]{
  \begin{center}
    \begin{circuitikz} \draw
      (0,0) -- (2,0) to[battery1, l_=$V_{Th}\eq#1$] (2,2) 
      to[resistor, l_=$R_{Th}\eq#2$] (0,2)
      ;
      \draw [o-] (-.07,2.079);
      \draw [o-] (-.07,0.079);
    \end{circuitikz}
  \end{center}
}

\newcommand{\norton}[2]{
  \begin{center}
    \begin{circuitikz} \draw
      (0,0) -- (3,0) to[american current source, l_=$I_{N}\eq#1$] (3,2) -- (0,2) (2,0)
      to[resistor, l=$R_{N}\eq#2$] (2,2)
      ;
      \draw [o-] (-.07,2.079);
      \draw [o-] (-.07,0.079);
    \end{circuitikz}
  \end{center}
}

\newcommand{\highlight}[1]{{\colorbox{yellow}{#1}}}

\newcommand{\ti}[1]{\widetilde{#1}}

\newfontface{\Kaufmann}{Kaufmann}
\DeclareTextFontCommand{\kf}{\Kaufmann}
\newcommand{\scriptr}{\fontsize{12pt}{12pt}\kf{r}}

\newfontface{\KaufmannB}{Kaufmann Bd BT}
\DeclareTextFontCommand{\kfb}{\KaufmannB}
\newcommand{\bscriptr}{\fontsize{12pt}{12pt}\kfb{r}}

\newcommand{\bv}[1]{\mathbf{#1}}

\title{Math 3160H: Assignment II}
\author{Jeremy Favro (0805980) \\ Trent University, Peterborough, ON, Canada}
\date{\today}

\begin{document}
\maketitle

% PROBLEM 1
\begin{problem}
A simple pendulum initially has the length $\ell_0$ and makes an angle $\theta_0$ with the vertical line. It is
then released from this position. If the length of the pendulum increases with time $t$ according to
$\ell=\ell_0+\epsilon t$, where $\epsilon$ is a small constant, then the angle $\theta$ that the pendulum makes with the vertical
line, assuming that the oscillations are small, is the solution of the initial value problem
$$\left(\ell_0+\epsilon t\right)\frac{d^2\theta}{dt^2}+2\epsilon\frac{d\theta}{dt}+g\theta=0,\quad \theta(0)=\theta_0,\quad \frac{d\theta}{dt}(0)=0$$
where $g$ is the acceleration due to gravity. Find the solution of the above initial value problem.
\end{problem}
\begin{soln}
  Consider the change of variables
  $$\ell-\ell_0=\epsilon t.$$ This gives us
  $d\ell=\epsilon dt$
  and so
  $$\frac{d\theta}{dt}=\epsilon\frac{d\theta}{d\ell}$$ and
  $$\epsilon^2\frac{d^2\theta}{d\ell^2}$$
  so the ODE becomes
  $$\ell \epsilon^2\frac{d^2\theta}{d\ell^2}+2\epsilon^2\frac{d\theta}{d\ell}+g\theta=0$$
  which we can divide through by $\epsilon^2$ to see that
  $$\ell\frac{d^2\theta}{d\ell^2}+2\frac{d\theta}{d\ell}+\frac{g}{\epsilon^2}\theta=0$$
  which is very nearly the Bessel equation, we just need to fiddle a bit more. We can multiply by $\ell$ to get
  $$\ell^2\frac{d^2\theta}{d\ell^2}+2\ell\frac{d\theta}{d\ell}+\frac{g\ell}{\epsilon^2}\theta=0$$
  which is of a form we know,
  $$x^2y''+(1-2a)xy'+\left[b^2c^2x^{2c}+(a^2-p^2c^2)\right]y=0$$
  which has solution
  $$y=c_1x^aJ_p(bx^c)+c_2x^aY_p(bx^c).$$
  Here then
  \begin{align*}
    1-2a     & =2            \\
    b^2c^2   & =g/\epsilon^2 \\
    2c       & =1            \\
    a^2-pc^2 & =0
  \end{align*}
  which tells us that
  \begin{align*}
    c & =1/2                \\
    a & =-1/2               \\
    b & =2\sqrt{g}/\epsilon \\
    p & =1
  \end{align*}
  so we get a solution of the form
  $$\theta(\ell)=c_1\ell^{-1/2}J_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)+c_2\ell^{-1/2}Y_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right).$$
  Now recall we originally had initial conditions in $t$ so we need to change those to $\ell$.
  Since $\ell=\ell_0+\epsilon t$, when $t=0$ $\ell=\ell_0$ so $\theta(t=0)=\theta_0\implies \theta(\ell=\ell_0)=\theta_0$
  and $\frac{d\theta}{dt}(t=0)=0\implies \frac{d\theta}{d\ell}(\ell=\ell_0)=0$.
  So we take the derivative of our solution,
  \begin{align*}
    \frac{d\theta(\ell)}{d\ell} & =\frac{d}{d\ell}\left[
      c_1\ell^{-1/2}J_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)
      +c_2\ell^{-1/2}Y_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)
    \right]                                                     \\
                                & =-\frac{1}{2\ell^{3/2}}\left[
      c_1J_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)
      +c_2Y_1\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)
    \right]                                                     \\
                                & \quad+
    \frac{\sqrt{g}}{2\epsilon\ell}\left[
      c_1\left[J_0\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)-J_2\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)\right]
      +c_2\left[Y_0\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)-Y_2\left(\frac{2\sqrt{g}}{\epsilon}\ell^{1/2}\right)\right]
    \right]                                                     \\
  \end{align*}
  and evaluate the condition,
  \begin{align*}
    \theta_\ell(\ell_0)=0
             & =-\frac{1}{2\ell_0^{3/2}}\left[
      c_1J_1\left(\frac{2\sqrt{g\ell_0}}{\epsilon}\right)
      +c_2Y_1\left(\frac{2\sqrt{g}}{\epsilon}\right)
    \right]                                    \\
             & \quad+
    \frac{\sqrt{g}}{2\epsilon\ell_0}\left[
      c_1\left[J_0\left(\frac{2\sqrt{g\ell_0}}{\epsilon}\right)-J_2\left(\frac{2\sqrt{g\ell_0}}{\epsilon}\right)\right]
      +c_2\left[Y_0\left(\frac{2\sqrt{g\ell_0}}{\epsilon}\right)-Y_2\left(\frac{2\sqrt{g\ell_0}}{\epsilon}\right)\right]
    \right]                                    \\
    \implies &
    c_2\left\{
    +\frac{1}{2\ell_0^{3/2}}Y_1\left(\frac{2\sqrt{g}}{\epsilon}\right)
    -\frac{\sqrt{g}}{2\epsilon\ell_0}\left[Y_0\left(\alpha\right)-Y_2\left(\alpha\right)\right]
    \right\}
    =
    c_1\left\{
    \frac{\sqrt{g}}{2\epsilon\ell_0}\left[J_0\left(\alpha\right)-J_2\left(\alpha\right)\right]
    -\frac{1}{2\ell_0^{3/2}}J_1\left(\alpha\right)
    \right\}
  \end{align*}
  for $\alpha=\frac{2\sqrt{g\ell_0}}{\epsilon}$
  Now we apply the condition $\theta(\ell=\ell_0)=\theta_0$,
  \begin{align*}
    \theta(\ell=\ell_0)=\theta_0 & =\frac{1}{\sqrt{\ell_0}}\left[c_1J_1\left(\alpha\right)+c_2Y_1\left(\alpha\right)\right] \\
    \implies                     & \frac{\sqrt{\ell_0}\theta_0-c_1J_1\left(\alpha\right)}{Y_1\left(\alpha\right)}=c_2.
  \end{align*}
  Since these coefficients are so long and nasty (and do not simplify, I tried in Sage) I will leave them in this form.
\end{soln}

% PROBLEM 2
\begin{problem}
The differential equation
$$x^2\frac{d^2E}{dx^2}-\mu x\frac{dE}{dx}-k^2x^\nu E=0$$
occurs in the study of the flow of current through electrical transmission lines; here $\mu$, $\nu$ are positive
constants and $E$ represents the potential difference (with respect to one end of the line) at a point a
distance $x$ from that end of the line. Find its general solution.
\end{problem}
\begin{soln}
  Recall we found that the ODE
  $$x^2y''+(1-2a)xy'+\left[b^2c^2x^{2c}+(a^2-p^2c^2)\right]y=0$$
  has solution
  $$y=c_1x^aJ_p(bx^c)+c_2x^aY_p(bx^c).$$
  The given problem is of this form with come of the coefficients being complex. We have that
  \begin{align*}
    1-2a       & =-\mu \\
    b^2c^2     & =-k^2 \\
    2c         & =\nu  \\
    a^2-p^2c^2 & =0
  \end{align*}
  which gives
  \begin{align*}
    a & =\frac{1}{2}(\mu+1)  \\
    b & =2i\frac{k}{\nu}     \\
    c & =\frac{\nu}{2}       \\
    p & =\frac{(\mu+1)}{\nu}
  \end{align*}
  so we get a solution of the form
  $$E=x^{\frac{1}{2}(\mu+1)}\left[c_1J_{\frac{(\mu+1)}{\nu}}(2i\frac{k}{\nu} x^{\frac{1}{2}\nu})+c_2Y_{\frac{(\mu+1)}{\nu}}(2i\frac{k}{\nu} x^{\frac{1}{2}\nu})\right].$$
\end{soln}

% PROBLEM 3
\begin{problem}~
\begin{enumerate}[label=(\alph*)]
  \item Show that the solution of the DE
        $$\frac{d^2w}{dt^2}+t^mw=0,\quad m>0$$
        can be expressed as
        $$w(t)=t^{1/2}\left(AJ_{1/(m+2)}\left(\frac{2}{m+2}t^{(m+2)/2}\right)+BJ_{-1/(m+2)}\left(\frac{2}{m+2}t^{(m+2)/2}\right)\right)$$
  \item Express the solution of DE
        $$\frac{d^2w}{dt^2}+w=0$$
        in terms of Bessel functions and deduce the relations given in Problem 9.2 Parts (i) and (ii).
\end{enumerate}
\end{problem}
\begin{soln}~
  \begin{enumerate}[label=(\alph*)]
    \item Multiplying through by $t^2$ this is again $$x^2y''+(1-2a)xy'+\left[b^2c^2x^{2c}+(a^2-p^2c^2)\right]y=0$$
          has solution
          $$y=c_1x^aJ_p(bx^c)+c_2x^aJ_{-p}(bx^c)$$
          for $p\notin \mathbb{Z}$. Here
          \begin{align*}
            1-2a       & =0   \\
            b^2c^2     & =1   \\
            2c         & =m+2 \\
            a^2-p^2c^2 & =0
          \end{align*}
          so
          \begin{align*}
            a & =\frac{1}{2} \\
            b & =2/(m+2)     \\
            c & =(m+2)/2     \\
            p & =1/(m+2)
          \end{align*}
          which indeed gives us the solution given in the problem statement.
    \item The ODE $$t^2\frac{d^2w}{dt^2}+t^2w=0$$
          has solution
          $$w(t)=c_1t^{1/2}J_{1/2}(t)+c_2t^{1/2}J_{-1/2}(t).$$
          Since the question just asks me to ``deduce''
          $$J_{1/2}(x)=\sqrt{\frac{2}{\pi x}}\sin x$$
          $$J_{-1/2}(x)=\sqrt{\frac{2}{\pi x}}\cos x$$
          we can note that the above ODE has solutions like $C_1\sin(x)+C_2\cos(x)$ and when plugging these in we see that
          $$\frac{d^2w}{dt^2}+w=-C_1\sin(x)-C_2\cos(x)+C_1\sin(x)+C_2\cos(x)=0$$
          and so indeed the relations must hold as we can multiply by $\sqrt{t}$. The $\sqrt{2/\pi}$ would have to come out from initial conditions
          as there is no normalization requirement to enforce that would imply it.
  \end{enumerate}
\end{soln}

% PROBLEM 4
\begin{problem} Use the definition of the Bessel functions to prove each of the following.
\begin{enumerate}[label=(\alph*)]
  \item $\displaystyle J_{-1/2}(x)=\sqrt{\frac{2}{\pi x}}\cos x$
  \item $\displaystyle J_{3/2}(x)=\sqrt{\frac{2}{\pi x}}\left(x^{-1}\sin x-\cos x\right)$
  \item $\displaystyle J_{-3/2}(x)=\sqrt{\frac{2}{\pi x}}\left(-x^{-1}\cos x-\sin x\right)$
\end{enumerate}
\end{problem}
\begin{soln} Recall that the series definition of the Bessel function is
  $$J_n(x) = \sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m+n+1)} {\left(\frac{x}{2}\right)}^{2m + n}$$
  \begin{enumerate}[label=(\alph*)]
    \item From the series definition
          \begin{align*}
            J_{-1/2}(x) & = \sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m+1/2)} {\left(\frac{x}{2}\right)}^{2m - 1/2}             \\
                        & = \sqrt{\frac{2}{x}}\sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m+1/2)} {\left(\frac{x}{2}\right)}^{2m}
          \end{align*}
          and $\Gamma(m+1/2)=\Gamma(m-1/2)(m-1/2)=\Gamma(m-3/2)(m-1/2)(m-3/2)\dots =\frac{\sqrt{\pi}(2m-1)(2m-3)\dots 3\cdot 1}{2^m}$ so
          \begin{align*}
            J_{-1/2}(x) & = \sqrt{\frac{2}{x}}\sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m+1/2)} {\left(\frac{x}{2}\right)}^{2m}                                 \\
                        & = \sqrt{\frac{2}{\pi x}}\sum_{m=0}^\infty \frac{(-1)^m}{m! (2m-1)(2m-3)\dots 3\cdot 1\cdot 2^m} x^{2m}                               \\
                        & = \sqrt{\frac{2}{\pi x}}\sum_{m=0}^\infty \frac{(-1)^m}{(m)(m-1)(m-2)\dots 3\cdot2\cdot1 (2m-1)(2m-3)\dots 3\cdot 1\cdot 2^m} x^{2m} \\
                        & = \sqrt{\frac{2}{\pi x}}\sum_{m=0}^\infty \frac{(-1)^m}{(2m)(2m-2)(2m-4)\dots 6\cdot 4\cdot 2 (2m-1)(2m-3)\dots 3\cdot 1} x^{2m}     \\
                        & = \sqrt{\frac{2}{\pi x}}\sum_{m=0}^\infty \frac{(-1)^m}{(2m)!} x^{2m}                                                                \\
                        & =\sqrt{\frac{2}{\pi x}} \cos x.
          \end{align*}
    \item From the series definition
          \begin{align*}
            J_{3/2}(x) & = \sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m+5/2)} {\left(\frac{x}{2}\right)}^{2m + 3/2}                                                  \\
                       & = \sum_{m=0}^\infty \frac{1}{2^{2m+1+1/2}}\frac{(-1)^m}{m!\Gamma(m+1/2+2)} x^{2m + 1+1/2}                                                 \\
                       & =\sqrt{\frac{2x}{\pi}}\sum_{m=0}^\infty \frac{(-1)^m}{(2m+1)!(2m+3)} x^{2m + 1}\justif{\quad}{Same process as previous to eval $\Gamma$.}
          \end{align*}
          Now I found this next step online as a general tip for working with Taylor series as I got stuck here. You can write
          $$1/(2m+3)=\int_{0}^{1}y^{2m+2}dy$$ which makes the series expansion
          \begin{align*}
            J_{3/2}(x) & = \sqrt{\frac{2x}{\pi}}\sum_{m=0}^\infty \frac{(-1)^m }{(2m+1)!(2m+3)} x^{2m + 1}                  \\
                       & = \sqrt{\frac{2x}{\pi}}\sum_{m=0}^\infty \frac{(-1)^m }{(2m+1)!} \int_{0}^{1}y^{2m+2}dy x^{2m + 1} \\
                       & = \sqrt{\frac{2x}{\pi}}\int_{0}^{1}y\sum_{m=0}^\infty \frac{(-1)^m }{(2m+1)!} y^{2m+1}x^{2m + 1}dy \\
                       & = \sqrt{\frac{2x}{\pi}}\int_{0}^{1}y\sum_{m=0}^\infty \frac{(-1)^m }{(2m+1)!} (xy)^{2m+1} dy       \\
                       & = \sqrt{\frac{2x}{\pi}}\int_{0}^{1}y\sin(xy) dy                                                    \\
                       & = \sqrt{\frac{2x}{\pi}}\frac{1}{x^2}\left[\sin(x)-x\cos(x)\right]                                  \\
                       & = \sqrt{\frac{2}{x\pi}}\left[x^{-1}\sin(x)-\cos(x)\right]                                          \\
          \end{align*}
    \item From the series definition
          \begin{align*}
            J_{-3/2}(x) & = \sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m-3/2+1)} {\left(\frac{x}{2}\right)}^{2m -3/2} \\
                        & = \sum_{m=0}^\infty \frac{(-1)^m}{m!\Gamma(m-1/2)} {\left(\frac{x}{2}\right)}^{2m -1/2-1} \\
          \end{align*}
          And I don't know where to go from here because $\Gamma(0-1/2)=\Gamma(-1/2)$ is undefined. I'll just show that the definition satisfies
          $$x^2y''+xy'+(x^2-(3/2)^2)y=0$$
          as this by definition has $J_{-3/2}(x)$ as a solution. Since this is just algebra we can pass it to Sage:
          \begin{tcolorbox}[breakable, size=fbox, boxrule=1pt, pad at break*=1mm,colback=cellbackground, colframe=cellborder]
            \prompt{In}{incolor}{1}{\boxspacing}
            \begin{minted}[breaklines, autogobble]{sage}
clear_vars()
x = var('x')
y = -sqrt(2/(pi*x))*((1/x)*cos(x)+sin(x))
ODE = x^2*diff(y, x, 2)+x*diff(y,x)+(x^2-(3/2)^2)*y
print("full: ")
show(ODE)
print("simplified: ")
show(ODE.full_simplify())
              \end{minted}
          \end{tcolorbox}
          \begin{tcolorbox}[breakable, size=fbox, boxrule=.5pt, pad at break*=1mm, opacityfill=0]
            \prompt{Out}{outcolor}{1}{\boxspacing}
            full:\\
            $\frac{1}{4} \, {\left(4 \, \sqrt{2} {\left(\frac{\cos\left(x\right)}{x} - \frac{2 \, \sin\left(x\right)}{x^{2}} - \frac{2 \, \cos\left(x\right)}{x^{3}} + \sin\left(x\right)\right)} \sqrt{\frac{1}{\pi x}} - \frac{4 \, \sqrt{2} {\left(\frac{\sin\left(x\right)}{x} + \frac{\cos\left(x\right)}{x^{2}} - \cos\left(x\right)\right)}}{\pi x^{2} \sqrt{\frac{1}{\pi x}}} - \frac{4 \, \sqrt{2} {\left(\frac{\cos\left(x\right)}{x} + \sin\left(x\right)\right)}}{\pi x^{3} \sqrt{\frac{1}{\pi x}}} + \frac{\sqrt{2} {\left(\frac{\cos\left(x\right)}{x} + \sin\left(x\right)\right)}}{\pi^{2} x^{4} \left(\frac{1}{\pi x}\right)^{\frac{3}{2}}}\right)} x^{2} - \frac{1}{4} \, \sqrt{2} {\left(4 \, x^{2} - 9\right)} {\left(\frac{\cos\left(x\right)}{x} + \sin\left(x\right)\right)} \sqrt{\frac{1}{\pi x}} + \frac{1}{2} \, {\left(2 \, \sqrt{2} {\left(\frac{\sin\left(x\right)}{x} + \frac{\cos\left(x\right)}{x^{2}} - \cos\left(x\right)\right)} \sqrt{\frac{1}{\pi x}} + \frac{\sqrt{2} {\left(\frac{\cos\left(x\right)}{x} + \sin\left(x\right)\right)}}{\pi x^{2} \sqrt{\frac{1}{\pi x}}}\right)} x$\\
            simplified:\\
            $0$
          \end{tcolorbox}
  \end{enumerate}
\end{soln}

% PROBLEM 5
\begin{problem}
In a study of planetary motion Bessel encountered the integral
$$y(x)=\frac{1}{\pi}\int_0^\pi\cos(n\theta-x\sin(\theta))d\theta$$
Show that $y(x)=J_n(x)$.
\end{problem}
\begin{soln}
  Recall that the generating function for $J_n$ is
  $$\exp(\frac{1}{2}x(h-\frac{1}{h}))=\sum_{n=-\infty}^\infty h^n J_n(x).$$
  If we take $h=e^{-i\theta}$ this becomes
  $$\exp(\frac{x}{2}(e^{-i\theta}-e^{i\theta}))=\sum_{n=-\infty}^\infty e^{in\theta} J_n(x)$$
  and
  $$\frac{x}{2}(e^{-i\theta}-e^{i\theta})=-ix\sin(\theta)$$ so
  $$\exp(-ix\sin\theta)=\sum_{n=-\infty}^\infty e^{-in\theta} J_n(x).$$
  Now both to get us closer to the integral formula above (whose integrand can be expressed using complex exponentials) and
  to use the orthogonality of $e^{imx}$ for $m\in\mathbb{Z}$ we multiply by $e^{im\theta}$,
  $$\exp(im\theta-ix\sin\theta)=\sum_{n=-\infty}^\infty e^{i(m-n)\theta} J_n(x)$$
  and integrate
  $$\int_{0}^{\pi}\exp(im\theta-ix\sin\theta)d\theta=\sum_{n=-\infty}^\infty \int_{0}^{\pi}e^{i(m-n)\theta}d\theta J_n(x).$$
  The integral
  $$\int_{0}^{\pi}e^{i(m-n)\theta}d\theta$$
  has two cases. When $m=n$, it is the integral of $1$ and comes out to $\pi$. When $m\neq n$ we get
  $$\int_{0}^{\pi}e^{ik\theta}d\theta=-i\frac{1}{k}\left[e^(ik\pi)-e^{-ik\pi}\right]=0$$
  where $k=m-n$ for simplicity. Hence we obtain a Kronecker delta function for $m$ and $n$ which eliminates all but the $m=n$ term
  in the sum, which will be multiplied by $\pi$. This leaves us with
  $$\int_{0}^{\pi}\exp(im\theta-ix\sin\theta)d\theta=\pi J_m(x).$$
  Which is almost what we want. Since
  $$\exp(im\theta-ix\sin\theta)=\cos(m\theta-x\sin\theta)+i\sin(m\theta-x\sin\theta),$$
  $$\int_{0}^{\pi}\cos(m\theta-x\sin\theta)+i\sin(m\theta-x\sin\theta)d\theta=\pi J_m(x).$$
  and since the right hand side is entirely real, the $\sin$ integral must vanish, leaving us with the desired result! Yay!
\end{soln}

% PROBLEM 6
\begin{problem}[SAGE]
\begin{enumerate}[label=(\alph*)]
  \item Plot $P_n(x)$ for $n = 0..5$ on the same graph.
  \item Find the Zeros of $P_{12}(x)$.
  \item Use SAGE to solve each of the following differential equations:
        \begin{enumerate}[label=(\roman*)]
          \item $\displaystyle (1-x^2)y''-2xy'+30y=0$;
          \item $\displaystyle (1-x^2)y''-2xy'+\left(30-\frac{16}{1-x^2}\right)y=0$.
        \end{enumerate}
  \item Use SAGE to plot $J_n(x)$ for $n = 0..5$ on the same graph. Comment on the behaviour of the
        graph.
  \item Use SAGE to plot $Y_n(x)$ for $n = 0..5$ on the same graph. Comment on the behaviour of the
        graph.
  \item Use SAGE to plot $K_n(x)$ for $n = 0..5$ on the same graph. Comment on the behaviour of the
        graph.
  \item Use SAGE to plot $I_n(x)$ for $n = 0..5$ on the same graph. Comment on the behaviour of the
        graph.
  \item Find the first three Zeros of $J_{1/2}(x)$ using SAGE.
  \item Find the first three Zeros of $Y_1(x)$ using SAGE.
  \item Use SAGE to solve each of the following differential equations:
        \begin{enumerate}[label=(\roman*)]
          \item $\displaystyle x^2y'' + xy' - (4x^2 + 9)y = 0$;
          \item $\displaystyle x^2y'' + xy' + (4x^2 - 9)y = 0$;
          \item $\displaystyle x^2y'' + 2xy' + (4x^2 -30)y = 0$.
        \end{enumerate}
\end{enumerate}
\end{problem}
\begin{soln}
  \includepdf[
    %% Include all pages of the PDF
    pages=-,
    %% make this page have the usual page style
    %% (you can change it to plain etc). By default pdfpages
    %% sets the pagecommand to \pagestyle{empty}
    pagecommand={\pagestyle{headings}},
    %% Add a "section" entry to the ToC with the heading
    %% "Quilling Shapes" and the label "sec:shapes"
    addtotoc={1,section,1,Quilling Shapes,sec:shapes}]
  %% The pdf file itself
  {./Sage/3160H A2.pdf}
\end{soln}
\end{document}